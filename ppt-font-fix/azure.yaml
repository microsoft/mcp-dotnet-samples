# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: ppt-font-fix

metadata:
  template: azd-init@1.14.0

services:
  ppt-font-fix:
    project: src/McpSamples.PptFontFix.HybridApp
    host: containerapp
    language: dotnet
    docker:
      path: ../../../Dockerfile.ppt-font-fix-azure
      context: ../../../
      remoteBuild: true
