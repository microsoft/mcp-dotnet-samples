# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: docx-to-hwpx

metadata:
  template: docx-to-hwpx@1.14.0

services:
  docx-to-hwpx:
    project: src/McpSamples.DocxToHwpx.HybridApp
    host: containerapp
    language: dotnet
    docker:
      path: ../../../Dockerfile.docx-to-hwpx-azure
      context: ../../../
      remoteBuild: true
